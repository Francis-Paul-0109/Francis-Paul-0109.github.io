---
title: "001. 데이터 저널리즘 쉽게 이해하기"
categories: Online journalism
layout: fullscreen
sidebar: false
footer: false
masthead: false
background_color: "#1A1A1A"
text_color: "#FFFFFF"
text_width: "90%"
text_margin_left: "15%"
text_padding_top: "0vh"
---

<section class="auto-scrolly">

  <div class="scrolly-text">

    <div class="step" data-img="data_intro.jpg">
      <em><strong>데이터 저널리즘이란?</strong></em>
      <p>데이터 기반으로 사실을 탐구하고, 그 사실을 독자에게 쉽게 전달하는 스토리텔링 저널리즘입니다.</p>
    </div>

    <div class="step" data-img="data_collection.jpg">
      <em><strong>1. 데이터 수집</strong></em>
      <p>뉴스, 공공 데이터, API 등 다양한 출처에서 <span class="highlight-keyword">데이터 수집</span>을 진행합니다.</p>
    </div>

    <div class="step" data-img="data_analysis.jpg">
      <em><strong>2️. 데이터 분석</strong></em>
      <p>수집한 데이터를 정리, 통계 처리, 패턴 분석 등을 통해 의미를 찾아냅니다.  
      핵심 과정: <span class="highlight-keyword">데이터 분석</span></p>
    </div>

    <div class="step" data-img="data_visualization.jpg">
      <em><strong>3️. 시각화</strong></em>
      <p>차트, 그래프, 지도 등으로 데이터를 시각적으로 표현하여 독자가 쉽게 이해하도록 합니다.  
      중요 포인트: <span class="highlight-keyword">시각화</span></p>
    </div>

    <div class="step" data-img="data_examples.jpg">
      <em><strong>실제 사례</strong></em>
      <ul>
        <li>The Guardian: 범죄 데이터 지도</li>
      </ul>
      데이터를 직관적으로 보여주어 국민 이해를 도왔습니다.
    </div>

    <div class="step" data-img="data_conclusion.jpg">
      <p>앞으로 뉴스 소비 시, 데이터를 어떻게 해석하고 시각화했는지 살펴보면 더욱 깊은 이해가 가능합니다.</p>
    </div>

  </div>

</section>

<!-- GSAP ScrollTrigger + 키워드 강조 애니메이션 -->
<script>
gsap.registerPlugin(ScrollTrigger);

const steps = document.querySelectorAll(".step");
const img = document.getElementById("auto-img");

// 초기 설정
steps.forEach((step, i) => {
  if(i === 0) step.classList.add("active");
});
img.src = `/assets/images/post/2.데이터저널리즘/${steps[0].dataset.img}`;

// 스크롤 트리거 설정
steps.forEach((step, index) => {
  ScrollTrigger.create({
    trigger: step,
    start: "top 60%",
    end: "bottom 40%",
    onEnter: () => activateStep(step),
    onEnterBack: () => activateStep(step)
  });
});

function activateStep(step) {
  // 이미지 변경
  const newImg = step.dataset.img;
  if(img.src.indexOf(newImg) === -1) {
    gsap.to(img, {opacity: 0, duration: 0.4, onComplete: () => {
      img.src = `/assets/images/post/2.데이터저널리즘/${newImg}`;
      gsap.to(img, {opacity: 1, duration: 0.4});
    }});
  }

  // 텍스트 활성화
  steps.forEach(s => s.classList.remove("active"));
  step.classList.add("active");

  // 텍스트 등장 애니메이션
  gsap.fromTo(step, {y: 30, opacity: 0}, {y: 0, opacity: 1, duration: 0.6, ease: "power2.out"});

  // 핵심 키워드 강조 애니메이션
  const kw = step.querySelectorAll(".highlight-keyword");
  kw.forEach(k => {
    gsap.fromTo(k, {scale: 0.8, color: "#ff9800", opacity: 0}, 
                        {scale: 1, color: "#ff9800", opacity: 1, duration: 0.6, ease: "back.out(1.7)"});
  });
}
</script>

<style>
.highlight-keyword {
  font-weight: bold;
  color: #ffffff;
}
</style>
